node_modules/
.env

.gradle/
/build/

# Archivos subidos en runtime (fotos de perfil, chat, evidencias, carnets, avistamientos) -
# se generan localmente al correr la app o los tests de integracion, nunca deben commitearse.
/uploads/

# IDE & local
.idea/
.vscode/
*.iml
local.properties

# Gradle/Kotlin caches
/bin/

# Credenciales de Firebase Admin SDK (nunca deben commitearse)
firebase-service-account.json
src/main/resources/firebase-service-account.json

