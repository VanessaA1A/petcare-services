const { hashPassword } = require('../src/utils/hash');
console.log(hashPassword(process.argv[2] || 'Pass1234'));

{
  "user"; { "id"; 1, "username"; "test1", "email"; "test1@example.com" }
  "session"; { "id"; 6, "token_sesion"; "UUID-TOKEN", "fecha_inicio"; "..." }
}
