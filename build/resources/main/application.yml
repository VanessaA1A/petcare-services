spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/PetCareBD
    username: postgres
    password: kelly123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never
  jackson:
    property-naming-strategy: SNAKE_CASE
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
jwt:
  secret: "YourJWTSecretKeyChangeThisToA32CharLongString!"
  expirationMs: 3600000

server:
  port: 8080
